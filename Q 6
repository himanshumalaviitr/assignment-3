#include<stdio.h>
#include<conio.h>
int main()
{
    int a,b;
    printf("Enter any two no.:-\n");
    scanf("%d%d",&a,&b);
    if(a<=b)
        printf("\ngreater no.= %d",b);
    getch();
    return 0;
}
